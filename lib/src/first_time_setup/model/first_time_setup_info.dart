import 'package:flutter/material.dart';

class FirstTimeSetupInfo {
  String name = '';
  TimeOfDay wakeUpTime = const TimeOfDay(hour: 6, minute: 0),
      sleepTime = const TimeOfDay(hour: 23, minute: 0);
}
