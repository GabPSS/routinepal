name: routinepal
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  routinepal_api:
    path: packages/routinepal_api
  routinepal_manager:
    path: packages/routinepal_manager
  bloc: ^9.0.0
  flutter_bloc: ^9.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
